FO5dwFsc0cbaIiH0h8J2eUks2vdTDwAn

```
# Many steps, using those commands
# unarchive
tar -xf file_name
gzip2 -d file_name
gzip -d file_name.gz

# change file type
mv file_name file_name.gz

# show file type
file file_name

# convert hex to bin
xxd -r hex_file > binary_file

```
