SSHKEY
```
nmap localhost -p 31000-32000 # not include ssl check/service version
nmap -sV localhost -p 31000-32000 # Include ssl check/service version - long time

openssl s_client -connect localhost:<port> -quiet # if not have quiet tag, it display "KEYUPDATE" only

```
